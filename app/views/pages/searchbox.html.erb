<!DOCTYPE html>
<html>
  <head>
  </head>
  <body style="font-family: Arial, Verdana, sans-serif; font-size: .8em;">
    <form action="http://findit.dtu.dk" method="GET" target="_blank">
      <input name="q" value="<%= params['q'] %>" size="40" style="width:<%= params['width'] || '300' %>px;">
      <input type="submit" value="<%= t('blacklight.search.form.submit')%>">
      <div style="margin-top:0.5em;">
        - <%= t 'toshokan.pages.searchbox' %>
      </div>
    </form>
  </body>
</html>
