<% tags.each do |tag| %>
  <span class="tag"><%= render_tag_value(tag) %> | <%= button_to 'x', document_tag_path(document, tag, :return_url => request.url), :method => :delete %></span>
<% end %>

<%= link_to t('toshokan.tags.add_tag'), new_document_tag_path(document, :return_url => request.url) %>
