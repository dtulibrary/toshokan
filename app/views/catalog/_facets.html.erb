<% # main container for facets/limits menu -%>
<div id="facets" class="facets sidenav">

  <h4 data-toggle="collapse" data-target=".facets-collapse">
    <a class="btn btn-navbar">
		    <span class="icon-bar"></span>
		    <span class="icon-bar"></span>
		  </a>
    <%= t('blacklight.search.facets.title') %>
  </h4>

  <div class="facets-collapse">
  <%= render_facet_partials %>
  <% if can? :logout, User %>
    <%= render_tag_partials %>
  <% end %>
  </div>
</div>


<% if can? :logout, User %>
  <div id="mylibrary" class="mylibrary sidenav">

    <h4>
      <%= t('toshokan.mylibrary.title') %>
    </h4>

    <div>
      <div>
	<h5><%= link_to t('toshokan.tags.bookmarks_and_tags'), :controller => "tags", :action => "manage" %></h5>
	<h5><%= link_to t('toshokan.mylibrary.more'), '#' %></h5>
      </div>
    </div>
  </div>
<% end %>

