<h1>Mixed suggestions</h1>
<%= form_tag search_action_url, :method => :get, :class => 'search-query-form form-inline clearfix' do %>
  <%= render_hash_as_hidden_fields(params_for_search().except(:q, :search_field, :qt, :page, :utf8)) %>
  <% unless search_fields.empty? %>
    <div class="pull-left hide">
      <label for="search_field" class="hide-text"><%= t('blacklight.search.form.search_field.label') %></label>
      <%= select_tag(:search_field, options_for_select(search_fields, h(params[:search_field])), :title => t('blacklight.search.form.search_field.title'), :class=>"search_field input-small") %>
      <span class="hide-text"><%= t('blacklight.search.form.search_field.post_label') %></span>
    </div>
  <% end %>
  <div class="text-field-with-icons">
    <%= text_field_tag :q, params[:q], :class => "search_q q", :id => "mixed_suggestions", data: { autocomplete_enabled: true, autocomplete_path: '/suggest'} %>
  <button type="submit" class="btn btn-primary search-btn" id="search">
    <span class="submit-search-text hidden-tablet"><%=t('blacklight.search.form.submit')%></span>
    <i class="icon-search hidden-desktop"></i>
  </button>
  </div>
<% end %>
<h1>Grouped suggestions</h1>
<%= form_tag search_action_url, :method => :get, :class => 'search-query-form form-inline clearfix' do %>
  <%= render_hash_as_hidden_fields(params_for_search().except(:q, :search_field, :qt, :page, :utf8)) %>
  <% unless search_fields.empty? %>
    <div class="pull-left hide">
      <label for="search_field" class="hide-text"><%= t('blacklight.search.form.search_field.label') %></label>
      <%= select_tag(:search_field, options_for_select(search_fields, h(params[:search_field])), :title => t('blacklight.search.form.search_field.title'), :class=>"search_field input-small") %>
      <span class="hide-text"><%= t('blacklight.search.form.search_field.post_label') %></span>
    </div>
  <% end %>
  <div class="text-field-with-icons">
    <%= text_field_tag :q, params[:q], :class => "search_q q", :id => "grouped_suggestions", data: { autocomplete_enabled: true, autocomplete_path: '/suggest'} %>
  <button type="submit" class="btn btn-primary search-btn" id="search">
    <span class="submit-search-text hidden-tablet"><%=t('blacklight.search.form.submit')%></span>
    <i class="icon-search hidden-desktop"></i>
  </button>
  </div>
<% end %>
